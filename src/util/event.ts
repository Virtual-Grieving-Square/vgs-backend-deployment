// import { io } from "../index";
// import { UserModel } from "../model/user";

// export const emitLikeUpdate = (userId: any, data: any) => {
//   io.emit("server_update_like", data);
// };

// export const emitcomment = (userId: any, data: any) => {
//   const user = UserModel.findById(userId);
//   if (!user) {
//   }
//   io.emit(user?.socketId, data);
// };
